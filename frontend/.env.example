# Development lokal: isi dengan URL backend lokal
VITE_API_BASE_URL=http://localhost:5000

# Production Azure (frontend + backend di App Service yang sama):
# Kosongkan agar API call menggunakan URL relatif ke domain yang sama
# VITE_API_BASE_URL=
